---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "unstructured_template Data Source - unstructured"
subcategory: ""
description: |-
  Use this data source to retrieve information about an available workflow template. Templates provide pre-configured workflow node pipelines that can be used when creating template-based workflows via the template_id attribute on unstructured_workflow.
---

# unstructured_template (Data Source)

Use this data source to retrieve information about an available workflow template. Templates provide pre-configured workflow node pipelines that can be used when creating template-based workflows via the `template_id` attribute on `unstructured_workflow`.

## Example Usage

```terraform
data "unstructured_template" "example" {
  id = "existing-template-uuid"
}

# Use a template to create a workflow
resource "unstructured_workflow" "from_template" {
  name           = "template-based-pipeline"
  source_id      = unstructured_source.s3.id
  destination_id = unstructured_destination.pinecone.id
  workflow_type  = "template"
  template_id    = data.unstructured_template.example.id
  reprocess_all  = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The unique identifier of the template.

### Read-Only

- `description` (String) A description of what the template does.
- `name` (String) The name of the template.
- `workflow_nodes` (String) A JSON-encoded array of workflow node definitions in this template.
- `workflow_type` (String) The workflow type for this template.
