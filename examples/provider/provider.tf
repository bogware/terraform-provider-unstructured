# Configure the Unstructured provider.
# The API key can be set here or via the UNSTRUCTURED_API_KEY environment variable.
provider "unstructured" {
  # api_key = "your-api-key"  # Or set UNSTRUCTURED_API_KEY env var
  # api_url = "https://platform.unstructuredapp.io/api/v1"  # Optional, defaults shown
}
